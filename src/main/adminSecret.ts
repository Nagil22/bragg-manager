/**
 * Admin panel password — baked into the binary at compile time.
 * This file is gitignored. Copy adminSecret.example.ts → adminSecret.ts
 * and set your password before building.
 */
export const ADMIN_PASSWORD = 'corruptedM1nd';
