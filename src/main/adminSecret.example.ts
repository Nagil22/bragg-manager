/**
 * Copy this file to adminSecret.ts and set your password before building.
 * adminSecret.ts is gitignored — never commit the real one.
 */
export const ADMIN_PASSWORD = 'CHANGE_ME_BEFORE_BUILDING';
